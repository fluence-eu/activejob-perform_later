name: RuboCop Measure (baseline)

on:
  push:
    branches: [main]

jobs:
  measure:
    uses: fluence-eu/ci-workflows/.github/workflows/ruby-gem-rubocop-measure.yml@v6
    permissions:
      contents: read
      packages: read
